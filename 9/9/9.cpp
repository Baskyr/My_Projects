#include <iostream>#include <vector>using namespace std;int main(){    vector <int>  a(3);       vector <double>  b;    cout << a.size() << endl;    cout << b.size() << endl;    for (unsigned int i = 0; i < a.size(); i++)        cout << a[i] << " ";   /* a[1] = 13;    a.at(2) = 24;    for (unsigned int i = 0; i < a.size(); i++)        cout << a[i] << " ";*/    for (auto x : a)        cout << x << " " << endl;}
